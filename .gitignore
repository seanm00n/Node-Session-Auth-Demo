# 의존성 패키지 (용량이 크고 재설치 가능하므로 제외)
node_modules/

# 환경 변수 파일 (보안 민감 정보 - API 키, DB 비번 등)
.env
.env.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 운영체제 관련 임시 파일
.DS_Store
Thumbs.db

# 에디터 관련 설정 (개인 설정이므로 제외 권장)
.vscode/
.idea/

# 빌드 결과물 (나중에 TypeScript 등을 쓸 경우)
dist/
build/